name: Android CI

on:
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: android-actions/setup-android@v3 
      - uses: gradle/actions/setup-gradle@v3

      - name: Lint
        run: ./gradlew lint

      - name: Compile
        run: ./gradlew assembleDebug

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint-and-build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: android-actions/setup-android@v3 
      - uses: gradle/actions/setup-gradle@v3

      - name: Unit tests
        run: ./gradlew test

      - name: Upload test report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-report
          path: app/build/reports/tests/
  # Uncomment the following jobs if you want to run UI tests as well. 
  # compose-ui-tests:
  #   runs-on: ubuntu-latest
  #   needs: unit-tests
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         java-version: '17'
  #         distribution: 'temurin'
  #     - uses: gradle/actions/setup-gradle@v3

  #     # Roboelectric tests -> No emulator needed, run in JVM
  #     - name: Compose UI tests (Robolectric)
  #       run: ./gradlew testDebugUnitTest

  # screenshot-tests:
  #   runs-on: ubuntu-latest
  #   needs: unit-tests
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         java-version: '17'
  #         distribution: 'temurin'
  #     - uses: gradle/actions/setup-gradle@v3

  #     - name: Paparazzi screenshot tests
  #       run: ./gradlew verifyPaparazziDebug